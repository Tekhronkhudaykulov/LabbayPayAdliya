export type LoginClientParamsType = {
    username: string;
    password: string;
    deviceName: string;
    ip?: string;
    token?: string
}

export type ClientType = {
    username?: string;
}